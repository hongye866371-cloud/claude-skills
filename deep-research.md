# Deep Research — 深度调研 Skill

## 触发方式
用户输入 `/deep-research <主题>` 启动。

## 第一步：问级别（必须先问，不能跳过）

向用户确认两个维度：

### 维度1：调研深度
| 级别 | 说明 | 适用场景 |
|------|------|---------|
| **轻量** | 市场扫描、竞品概览、快速结论 | 做决策前的初步了解 |
| **中度** | 有数据支撑、有对比分析、可引用 | 写spec/PRD/设计文档 |
| **论文级** | 学术文献+理论框架+研究缺口+方法论 | 发论文、学术演讲、投资人级报告 |

### 维度2：调研方向
| 方向 | 说明 |
|------|------|
| **深度优先** | 串行递进，层层深挖，每步基于上一步结论 |
| **广度优先** | 并行展开，覆盖多个独立方向 |

使用 AskUserQuestion 工具让用户选择。

## 第二步：出方案（必须等用户确认才能执行）

根据用户选择的级别和方向，给出具体方案：
- 分几个Phase
- 每Phase做什么
- 预估开几个agent
- 预计产出什么文件

方案必须简洁，用表格呈现，不超过20行。

展示方案后等用户说"好"或调整，**不能自动开始执行**。

## 第三步：执行调研

### 四道防爆锁（强制，所有级别都生效）

#### 锁0：禁止嵌套锁
**agent内部禁止再开子agent。** 这是最高优先级规则，违反会瞬间耗光token。
- 每个agent的prompt中必须包含这句话：「你是一个独立研究员。禁止使用Agent工具。禁止开子agent。所有工作必须你自己完成。」
- 如果一个Phase需要多个搜索方向，由主loop串行或并行开多个agent，**不能让agent自己决定开几个**
- 主loop控制agent数量，agent只负责执行单一任务并返回结果

#### 锁1：Schema锁
每个agent必须使用schema返回结构化数据。禁止agent返回自由长文。
- agent把详细内容直接 Write 到磁盘文件
- 只通过schema返回摘要级信息（citation、keyPoint、filePath等）
- 单个agent返回给主上下文的数据不超过500 token

#### 锁2：压缩锁
每个Phase结束时，主loop必须：
1. 从agent返回的结构化数据中提取关键发现
2. 压缩成不超过300字的brief
3. 展示brief给用户，等用户确认方向或调整
4. 只把brief传递给下一个Phase

#### 锁3：磁盘锁
- 所有详细内容（分析、长文、HTML）由agent直接Write到磁盘
- 主上下文永远不 Read 超过200行的文件
- 需要编辑大文件时，使用 offset+limit 只读目标行范围
- 最终文档的拼接由agent在隔离上下文中完成

### 文件组织
所有调研产出写入 `reviews/` 目录：
- 过程文件：`reviews/research-{topic}/phase{N}-{label}.md`
- 最终文档：`reviews/{date}-{topic}.html`

### Phase间流程
```
Phase N 执行 → agent写盘+返回schema → 主loop压缩brief → 展示给用户 → 用户确认 → Phase N+1
```

用户在任何Phase间都可以：
- 说"继续"→ 进入下一Phase
- 说"这条不要丢"→ 主loop把该条加入brief
- 说"加一个方向"→ 调整下一Phase的agent prompt
- 说"够了"→ 直接进入写作Phase

## 级别对应的默认Phase结构

### 轻量
1. **探测**：1-2个agent扫描 → brief
2. **写作**：1个agent直接写报告到磁盘

### 中度
1. **探测**：2-3个agent并行搜索 → brief
2. **分析**：1-2个agent深入分析关键发现 → brief
3. **写作**：按章节分agent写盘

### 论文级
1. **探测**：2-3个agent并行（文献/竞品/语料）→ brief
2. **深挖**：串行agent精读核心论文 → brief
3. **建构**：单agent设计理论框架/模型 → brief
4. **补缺**：针对research gap补充调研 → brief
5. **写作**：按章节分agent写盘 → 拼接agent组装最终HTML

以上为默认结构，用户确认方案时可增删Phase。

## 注意事项
- agent数量不硬性规定，根据主题和级别灵活决定，在方案中明确
- 深度来自Phase间的串行递进，不是agent数量
- 广度来自Phase内的并行展开，由用户prompt控制
- 每个Phase结束必须展示brief并等用户确认，这是防止方向偏离的关键
- 如果某个Phase的agent返回了意外发现（schema之外的重要信息），在brief中标注"意外发现"提请用户注意
